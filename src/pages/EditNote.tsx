import React from "react";
import {useParams} from "react-router-dom";

const EditNote = () => {
  const {id} = useParams();
  // const note =
  
  return (
	<div>
	  EditNote
	</div>
  );
};

export default EditNote;
